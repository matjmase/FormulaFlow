import { AuthorizedIfDirective } from './authorized-if.directive';

describe('AuthorizedIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedIfDirective();
    expect(directive).toBeTruthy();
  });
});
